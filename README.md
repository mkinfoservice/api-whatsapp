# api-whatsapp

## Variáveis de ambiente

Adicione as seguintes variáveis de ambiente ao seu arquivo `.env` ou ao ambiente de execução:

- `VERIFY_TOKEN`: uma string qualquer usada para validar chamadas (por exemplo, `vibecode`).
